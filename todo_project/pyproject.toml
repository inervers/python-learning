[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
description = "一个简单的待办清单CLI工具"
requires-python = ">=3.10"

[project.scripts]
todo = "todo.cli:main"
